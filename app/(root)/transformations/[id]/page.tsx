import React from "react";

const TransformationsPage = () => {
  return <div> TransformationsPage</div>;
};

export default TransformationsPage;

//http://localhost:3000/transformations pe nhi hoga
//http://localhost:3000/transformations/[id] to implement this we will use concept of dynamic routes
// for that we will make a [id] folder in transformations and keep the page.tsx file in that
